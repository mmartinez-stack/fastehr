packages:
  - apps/*
  - packages/*

allowBuilds:
  # prisma downloads its query engine binaries in a postinstall script
  '@prisma/client': true
  '@prisma/engines': true
  prisma: true
  # native bindings for eslint-import-resolver-typescript
  unrs-resolver: true
