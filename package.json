{
  "name": "fastehr",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@11.21.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "generate": "turbo run generate",
    "check:graph": "node scripts/check-task-graph.mjs"
  },
  "devDependencies": {
    "turbo": "2.10.9",
    "typescript": "5.7.3"
  }
}
